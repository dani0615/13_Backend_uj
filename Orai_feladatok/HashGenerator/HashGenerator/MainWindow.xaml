<Window x:Class="HashGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hash Generator Pro"
        Height="680" Width="1000"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#121212"
        FontFamily="Segoe UI Variable Display, Segoe UI">

    <Window.Resources>
        <DropShadowEffect x:Key="CardShadow" Color="Black" Direction="270" ShadowDepth="8" BlurRadius="25" Opacity="0.7"/>
        <DropShadowEffect x:Key="TextBoxShadow" Color="Black" Direction="270" ShadowDepth="3" BlurRadius="12" Opacity="0.5"/>

        <!-- TextBox és PasswordBox stílusok -->
        <Style x:Key="ModernTextBoxBase" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Effect" Value="{StaticResource TextBoxShadow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    CornerRadius="10" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                            <Border x:Name="focusBorder" CornerRadius="10" BorderThickness="2"
                                    BorderBrush="#0078D4" Opacity="0" IsHitTestVisible="False"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="focusBorder" Property="Opacity" Value="1"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBoxBase}"/>
        <Style TargetType="PasswordBox" BasedOn="{StaticResource ModernTextBoxBase}"/>

        <Style x:Key="OutputTextBox" TargetType="TextBox" BasedOn="{StaticResource ModernTextBoxBase}">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="#252529"/>
            <Setter Property="Foreground" Value="#8AB4F8"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#B0B0B0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,24,0,4"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,14"/>
            <Setter Property="Margin" Value="0,20,0,0"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12"
                                Effect="{StaticResource CardShadow}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1090F0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Fő Grid -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Menü -->
            <RowDefinition Height="Auto"/>
            <!-- Cím -->
            <RowDefinition Height="*"/>
            <!-- Tartalom -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Felső menü -->
        <Menu Grid.Row="0" Background="#1E1E21" Foreground="#E0E0E0" FontSize="13">
            <MenuItem Header="Fájl">
                <MenuItem Header="Mentés másként..." Click="Menu_SaveAs_Click" InputGestureText="Ctrl+S"/>
                <Separator/>
                <MenuItem Header="Kilépés" Click="Menu_Exit_Click" InputGestureText="Alt+F4"/>
            </MenuItem>
            <MenuItem Header="Segítség">
                <MenuItem Header="Névjegy" Click="Menu_About_Click"/>
            </MenuItem>
        </Menu>

        <!-- Cím -->
        <TextBlock Grid.Row="1" Text="Hash Generator Pro" 
                   FontSize="40" FontWeight="Light" Foreground="#E0E0E0"
                   HorizontalAlignment="Center" Margin="0,20,0,40"/>

        <!-- Tartalom -->
        <Grid Grid.Row="2" Margin="40,0,40,40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1.1*"/>
            </Grid.ColumnDefinitions>

            <!-- Bal oldal -->
            <Border Background="#1E1E21" CornerRadius="18" Padding="32" Effect="{StaticResource CardShadow}">
                <StackPanel>
                    <TextBlock Text="Bemeneti adatok" FontSize="18" Foreground="#8AB4F8" Margin="0,0,0,20"/>
                    <Label Content="Login name "/>
                    <TextBox x:Name="tbxLogin"/>
                    <Label Content="Jelszó"/>
                    <TextBox x:Name="tbxPassword"/>
                    <Button Content="Hash generálása →" Name="btnGeneralas" Click="btnGeneralas_Click" HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>

            <!-- Jobb oldal -->
            <Border Grid.Column="1" Background="#1E1E21" CornerRadius="18" Padding="32" Margin="24,0,0,0"
                    Effect="{StaticResource CardShadow}">
                <StackPanel>
                    <TextBlock Text="Generált értékek" FontSize="18" Foreground="#8AB4F8" Margin="0,0,0,20"/>
                    <Label Content="Salt (véletlen generált)"/>
                    <TextBox x:Name="tbxSalt" Style="{StaticResource OutputTextBox}" Height="46"/>
                    <Label Content="Hash (SHA-256 + Salt)"/>
                    <TextBox x:Name="tbxHash" Style="{StaticResource OutputTextBox}" Height="90"/>
                    <Label Content="Double Hash (SHA-256(SHA-256 + Salt))"/>
                    <TextBox x:Name="tbxDoubleHash" Style="{StaticResource OutputTextBox}" Height="90"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Footer -->
        <TextBlock Grid.Row="3" Text="Kandó Biztonsági • Rendszer • 2025" Foreground="#555555" FontSize="11"
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20"/>
    </Grid>
</Window>